<%# Recent Athlete Item Partial %>
<div class="flex gap-3 p-3 rounded-lg bg-gradient-to-r from-primary-50/50 to-white border-l-4 border-l-primary-500 hover:shadow-sm transition-all">
  <div class="flex items-center" aria-hidden="true"><%= icon('👤', size: 'lg', aria_hidden: true) %></div>
  <div class="flex-1 min-w-0">
    <p class="text-sm font-medium text-neutral-900"><%= athlete.name %></p>
    <p class="text-xs text-neutral-600 mt-1">Entrou em <%= athlete.created_at.strftime("%d/%m/%Y") %></p>
  </div>
</div>

