<% content_for :title, "Novo Jogo" %>

<div class="grid grid-cols-1 md:grid-cols-12 gap-6">
  <!-- Header Section -->
  <header class="md:col-span-12">
    <nav class="mb-4" aria-label="Breadcrumb">
      <%= link_to matches_path, 
          class: "text-sm text-neutral-600 hover:text-neutral-900 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 rounded-md px-2 py-1 transition-colors",
          aria: { label: "Voltar para lista de jogos" } do %>
        <span class="mr-1" aria-hidden="true">←</span>
        <span>Voltar para jogos</span>
      <% end %>
    </nav>
    <h1 class="text-3xl font-bold mb-2">Novo Jogo</h1>
    <p class="text-sm text-neutral-600">Preencha os dados abaixo para agendar um novo jogo no sistema</p>
  </header>

  <!-- Form Section -->
  <section class="md:col-span-12">
    <div class="rounded-lg bg-white border border-neutral-200 p-6">
      <h2 class="sr-only">Formulário de Cadastro</h2>
      <%= render "form", match: @match %>
    </div>
  </section>
</div>
